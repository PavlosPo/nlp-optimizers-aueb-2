\textbf{\emph{Full Mode}}

In the Full Mode, where additional hyperparameters were tuned for each optimizer, we observe an overall improvement in performance for both adaptive and non-adaptive optimizers. Specifically, there is a significant boost in ROUGE-1 and ROUGE-2 (F-measure) scores (see Tables \ref{samsum_test_results_1_full_training} and \ref{samsum_test_results_2_full_training}). For instance, the ROUGE-1 score for NAdam increased from $0.3094$ to $0.4527$, representing a $46\%$ improvement, with a similar increase observed in ROUGE-2. This trend is also evident for AdamW and Adam. In contrast, while the SGDM and SGD optimizers show improved Test Loss scores, they still report zero values for most other metrics. The exception is SGDM, which achieved a low but measurable F1 BERTScore of $0.00017$.

% First table with the first 3 optimizers
\begin{table}[H]
    \centering
    \caption{This table presents the mean values of each optimizer's performance, averaged over four different seeds, for the SAMSum dataset in Full Mode. The table includes results for each optimizer's hyperparameters and learning rates. Due to space constraints, this is Part 1 of 2.}
    \begin{tabular}{>{\raggedright\arraybackslash}p{0.3\textwidth} *{3}{>{\centering\arraybackslash}p{0.2\textwidth}}}
    \toprule
    \textbf{Metrics} & NAdam & SGD & SGDM \\
    \midrule
    Test Loss & ${0.0943}$ ($\pm$${0.001}$) & ${0.2015}$ ($\pm$${0.0016}$) & ${0.1552}$ ($\pm$${0.0057}$) \\
    Test F1 (BERTScore) & ${0.553}$ ($\pm$${0.001}$) & ${0}$ & ${0.00017}$ ($\pm$${0.0003}$) \\
    Test Rouge 1 (F Measure) & ${0.4527}$ ($\pm$${0.0065}$) & ${0}$ & ${0}$ \\
    Test Rouge 2 (F Measure) & ${0.215}$ ($\pm$${0.0066}$) & ${0}$ & ${0}$ \\
    \bottomrule
    \end{tabular}
    \label{samsum_test_results_1_full_training}
\end{table}
    
% Second table with the last 2 optimizers
\begin{table}[H]
    \centering
    \caption{This table continues from Part 1, showing the mean values of each optimizer's performance, averaged over four seeds, for the SAMSum dataset in Full Mode. This table captures additional hyperparameters and optimizers.}
    \begin{tabular}{>{\raggedright\arraybackslash}p{0.3\textwidth} *{2}{>{\centering\arraybackslash}p{0.2\textwidth}}}
    \toprule
    \textbf{Metrics} & AdamW & Adam \\
    \midrule
    Test Loss & ${0.0948}$ ($\pm$${0.0008}$) & ${0.0944}$ ($\pm$${0.0008}$) \\
    Test F1 (BERTScore) & ${0.553}$ ($\pm$${0.0008}$) & ${0.5558}$ ($\pm$${0.0038}$) \\
    Test Rouge 1 (F Measure) & ${0.4495}$ ($\pm$${0.0042}$) & ${0.4521}$ ($\pm$${0.0027}$) \\
    Test Rouge 2 (F Measure) & ${0.2126}$ ($\pm$${0.0023}$) & ${0.2127}$ ($\pm$${0.0031}$) \\
    \bottomrule
    \end{tabular}
    \label{samsum_test_results_2_full_training}
\end{table}